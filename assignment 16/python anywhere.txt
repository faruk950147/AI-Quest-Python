# 1. Clone your repo (skip if already cloned)
git clone https://github.com/faruk950147/Deployment.git

# 2. Create virtualenv with Python 3.13
mkvirtualenv --python=/usr/bin/python3.13 env

# 3. Activate virtualenv
workon env

# 4. Go to your project folder
cd Deployment

# 5. Install requirements
pip install -r requirements.txt

# 6. Run database migrations
python manage.py migrate

# 7. Collect static files
python manage.py collectstatic
# (When prompted, type 'yes' and press Enter)

9. web tab -> virtualenv
/home/faruk950147/.virtualenvs/env

 


import sys
import os

# project path
path = '/home/faruk950147/deployment'
if path not in sys.path:
    sys.path.append(path)

os.environ['DJANGO_SETTINGS_MODULE'] = 'config.settings'  # তোমার settings file অনুযায়ী পরিবর্তন করতে হবে

from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
